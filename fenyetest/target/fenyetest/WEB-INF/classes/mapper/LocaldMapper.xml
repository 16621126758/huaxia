<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.chnsys.dao.LocaldMapper">
  <!--<select id="selectBlog" resultType="Blog">-->
  <!--select * from Blog where id = #{id}-->
  <!--</select>-->

  <insert id="insertLocal" parameterType="cn.com.chnsys.pojo.Local123">
    insert into local (local_date,local_time,local_date_time)
    values (#{localDate1},#{localTime1},#{localDateTime1});
  </insert>
</mapper>